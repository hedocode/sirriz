let serverPort = 443
let serverURL= "http://localhost:" + serverPort + "/";
let server_API_URL= serverURL + "api/";

module.exports = {
    serverPort: serverPort,
    serverURL: serverURL,
    server_API_URL: server_API_URL
}